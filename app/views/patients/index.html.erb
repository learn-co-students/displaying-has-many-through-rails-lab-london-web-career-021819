<h1>Patients:</h1>
<ul>
<% @patients.each do |pat| %>

  
    <li>
      <h3>Name: <%= link_to pat.name, patient_path(pat) %> Number of Appointments: <%= pat.appointments.count %>  </h3>
    </li>
  
  


<% end %>
</ul>